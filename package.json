{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "prepare:images": "node scripts/prepare-images.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:fairhousing": "node scripts/check-fairhousing.mjs",
    "check": "npm run build && npm run check:links && npm run check:fairhousing"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.10",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/sitemap": "^3.7.4",
    "sharp": "^0.35.4"
  }
}
